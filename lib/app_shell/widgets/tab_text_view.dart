import 'package:flutter/material.dart';

class TabTextView extends StatelessWidget {
  const TabTextView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Simple Text Tab", style: TextStyle(fontSize: 20)),
    );
  }
}
